import buscar from './buscar.vue'
import editar from './editar.vue'

const routes = [
                {path:'/', component:buscar },
                {path:'/editar', component:editar }
            ]

export default routes